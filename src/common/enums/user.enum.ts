export enum UserRole {
  ADMIN = 'admin',
  MODERATOR = 'moderator',
  OWNER = 'owner',
  EMPLOYEE = 'employee',
  CLIENT = 'client',
  USER = 'user',
}

export enum UserStatus {
  ACTIVE = 'active',
  BANNED = 'banned',
  DELETED = 'deleted',
  INACTIVE = 'inactive',
}
